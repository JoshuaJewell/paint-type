// SPDX-License-Identifier: CC-BY-SA-4.0

= container
:description: Container configuration and images

This directory is part of the paint.type project.

== Purpose

Container configuration and images.


As a Level 1 directory (`container/`), it supports the overall
project structure.

== Contents by Functional Grouping\n\n=== Configuration\n\n* `compose.toml` — Configuration file\n* `.gatekeeper.yaml` — YAML configuration\n* `ct-build.sh` — Shell script\n* `manifest.toml` — Configuration file\n* `entrypoint.sh` — Shell script\n* `compose.example.toml` — Configuration file\n* `vordr.toml` — Configuration file\n\n=== Other Files\n\n* `deploy.k9.ncl` — deploy.k9 configuration or data\n* `Containerfile` — Container image definition\n\n
== Purpose of Each Component

Each file in this directory serves a specific purpose.

*Configuration files* define build, test, and deployment parameters.\n
*Other files* support directory operations.\n

== AI Manifest Structure

* `0.1-AI-MANIFEST.a2ml` (this file) - Level 1 manifest for `container/`



== Related Files

* link:../0-AI-MANIFEST.a2ml[Parent AI Manifest]
* link:../README.adoc[Parent Directory README]
* link:../../.machine_readable/[Machine Readable Metadata]
