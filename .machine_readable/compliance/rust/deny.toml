# SPDX-License-Identifier: AGPL-3.0-or-later
# cargo-deny configuration for RSR-compliant repositories
# Run: cargo deny check
# Docs: https://embarkstudios.github.io/cargo-deny/

[graph]
targets = []
all-features = true

# --- Advisory database ---------------------------------------------------
[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
# Fail on any known vulnerability
vulnerability = "deny"
unmaintained = "warn"
yanked = "warn"
notice = "warn"

# --- License policy -------------------------------------------------------
[licenses]
unlicensed = "deny"
confidence-threshold = 0.8

allow = [
    "AGPL-3.0-or-later",
    "MPL-2.0",
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "Unicode-3.0",
    "Unicode-DFS-2016",
]

deny = [
    # paint.type itself is AGPL-3.0-or-later, so allow it (above). Block
    # GPL-3.0 / GPL-2.0 dependencies that would create stricter combined
    # obligations than AGPL imposes on its own.
    "GPL-3.0-only",
    "GPL-3.0-or-later",
    "GPL-2.0-only",
    "GPL-2.0-or-later",
]

copyleft = "allow"

[[licenses.exceptions]]
allow = ["OpenSSL"]
name = "ring"

# --- Crate bans ------------------------------------------------------------
[bans]
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"

deny = [
    # Known-bad crates
    { name = "openssl", wrappers = ["openssl-sys"] },
]

# --- Source restrictions ----------------------------------------------------
[sources]
unknown-registry = "deny"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
